# External port (Docker) / listen port (npm start)
PORT=8088

# Listen host (npm start). Docker will override to 0.0.0.0
HOST=127.0.0.1

# Admin console login
ADMIN_PASSWORD=change-me
JWT_SECRET=change-me-too

# Optional:
# DB_PATH=./data/database.sqlite
# ADMIN_PASSWORD_BEARER_COMPAT=true
# MAX_CONCURRENT_PER_ACCOUNT=1
# MAX_CONCURRENT_PER_MODEL=3
